# Createrington server SFTP connection
SFTP_HOST=gamesfra1113.bisecthosting.com
SFTP_PORT=2022
SFTP_USERNAME=
SFTP_PASSWORD=

# Remote path to the kubejs folder on the Createrington server
REMOTE_KUBEJS_PATH=/kubejs

# Backup path — the remote kubejs folder is renamed here before each push.
# If not set, no backup is created.
REMOTE_BACKUP_PATH=/kubejs-backup
